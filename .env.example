FRONTEND_PORT=8080
# Backend URL nginx proxies /api/ to (full URL including scheme).
# Dev mode: Vite proxies /api to BACKEND_URL (see server.proxy in vite.config.ts).
# The host cannot reach host.docker.internal, so for local dev override it in .env.development:
BACKEND_URL=http://host.docker.internal:8000
