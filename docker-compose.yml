services:
  frontend:
    build:
      context: .
    ports:
      - '${FRONTEND_PORT:-8080}:80'
    environment:
      - BACKEND_URL=${BACKEND_URL:-http://host.docker.internal:8000}
    restart: unless-stopped
